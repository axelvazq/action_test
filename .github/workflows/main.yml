name: Test action

on:
  pull_request:
    branches:
    - dev
    types:
    - opened
    - synchronize
    - closed

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Load env
      uses: axelvazq/action_test/.github/workflows/setup-env.yml@feature/pr
      with:
        environment: DEV